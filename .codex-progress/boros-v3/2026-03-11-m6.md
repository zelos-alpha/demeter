goal: 把 settlement 与 convergence 信号的时间口径统一到 `latestFTime`

changes:
- `market.py` 的持仓 present value 改为按 `latest_f_time_to_maturity_seconds`
- `strategy.py` 的 `signal_ready` 时间门槛改为优先使用 `latest_f_time_to_maturity_seconds`
- 让 settlement、执行、信号三层都围绕同一 window 时间基准

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest -q tests/boros_v4_convergence_test.py tests/boros_v4_backtest_test.py`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- `findex_updated` 的真实 floating index / fee index 字段仍未恢复
- settlement fee 仍是 proxy
- 订单簿/AMM 执行仍是 taker-only experimental

next_step:
- 继续确认 `findex_updated` 里的 index 数值字段
- 若确认成功，用真实 index 替换当前 continuous proxy index
