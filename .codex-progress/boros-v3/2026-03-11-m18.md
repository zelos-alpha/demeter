goal: build a minimal full Boros execution prototype and compare it against the current tx replay path

changes:
- added an experimental `EVENT_REPLAY_FULL_PROTO` execution mode
- built `mark_rate_full_proto` from event-level implied-rate fills with a trailing observation window
- wired markets to switch mark-rate columns by execution mode
- added a comparison sample at `samples/strategy-example/78_boros_full_execution_compare.py`
- generated `outputs/boros_full_execution_compare/comparison.json` and `report.md`

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/tests -p 'boros_v4_*test.py'`
- result: `14 tests OK`

known gaps:
- the full execution prototype still approximates mark observation windows; it is not a full protocol `markRateView`
- the compare sample is expensive to run end-to-end on the full dataset

next step:
- decide whether the ~20% runtime hit for the observed pnl improvement is worth productizing
- if yes, push more protocol execution details into the experimental path
