goal: 在真实 index 字段尚未恢复前，先把回测估值与信号逻辑完全统一到 `latestFTime` 时间基准

changes:
- `market.py` 的 present value 改为按 `latest_f_time_to_maturity_seconds`
- `strategy.py` 的 `signal_ready` 改为优先使用 `latest_f_time_to_maturity_seconds`
- 实测确认这不会破坏现有 convergence 链路

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest -q tests/boros_v4_convergence_test.py tests/boros_v4_backtest_test.py`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- `findex_updated` 真实 floating index / fee index 仍未恢复
- 当前数据里没有 `fee_rate_updated` 事件，settlement fee 仍是 proxy
- Hyperliquid 末端小时级长尾虽然被压缩，但真实 index 仍缺失

next_step:
- 继续恢复 `findex_updated` 的数值字段
- 若无法稳定恢复，增加一个更保守的 event-window settlement mode，避免继续高估 near-maturity 信号
