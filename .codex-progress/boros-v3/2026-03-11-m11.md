goal: 给双市场 convergence 策略加入最小的 edge-after-cost 信号门槛，先过滤掉明显负边际交易

changes:
- `FundingConvergenceStrategy` 新增 `expected_holding_seconds` 和 `min_expected_edge_after_cost`
- 开仓前先估算：
  - gross spread reversion edge
  - opening execution fee / opening fee proxy
  - settlement fee proxy
- 只有 `expected_edge_after_cost >= threshold` 才允许开仓
- `TX_REPLAY_BEST_EXEC` 执行拆成 `peek` / `claim`，避免信号评估阶段提前消费 tx
- sample `72_boros_funding_convergence.py` 默认启用一个保守 gate

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/tests -p 'boros_v4_*test.py'`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python /Users/hou/Desktop/demeter/samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- edge 仍是基于当前 implied spread 的近似收益，不是完整 forward PnL 预测
- settlement fee 仍然不是 oracle config 直接恢复

next_step:
- 把 gate 相关统计导入 diagnostics/report
- 再测试更严格的 holding horizon / edge threshold，找更稳的研究口径
