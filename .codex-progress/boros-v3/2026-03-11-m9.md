goal: 用官方 Boros 合约的 `FIndex` ABI 恢复真实 `floating_index / fee_index`，让 settlement 不再依赖 proxy accrual

changes:
- 对照官方 `@pendle/boros-core` 合约确认 `FIndex = uint32 fTime + int112 floatingIndex + uint64 feeIndex`
- `helper.py` 的 `findex_updated` decoder 改为按真实 `bytes26 newIndex` 解码，并把第二个 word 作为 `newFTag`
- 分钟级 `state_frame` 改为优先继承真实 `floating_index / fee_index`
- 结算费年化列改为由真实 `fee_index` 邻接差分反推，而不是继续按窗口 proxy 积分
- convergence fixture 改为输出真实 `FIndex` 编码，而不是 dummy payload

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/tests -p 'boros_v4_*test.py'`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python /Users/hou/Desktop/demeter/samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- 当前双市场数据里仍没有 FIndex oracle config 事件，所以 settlement fee rate 只能由 fee index 差分反推
- `spread_last` 仍然有 near-maturity 长尾，说明策略信号层后面还要继续收紧
- execution 仍是 taker-only experimental replay，不是完整 maker/orderbook 仿真

next_step:
- 评估是否把 `fee_index` 差分得到的 settlement fee rate 直接暴露进报告
- 再继续压 near-maturity signal tail，并补 README/限制说明
