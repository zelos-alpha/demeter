goal: 用 `findex_updated` payload 的 `latestFTime` 替换事件时间近似，修正 settlement timeline

changes:
- 在 `helper.py` 中新增 `findex_updated` 解码，正式提取 `latest_f_time`
- `load_boros_event_data(...)` 现在按 payload 里的 `latest_f_time` 建 state frame
- 新增 `latest_f_time_to_maturity_seconds`
- `market.py` 开仓 upfront fixed cost / opening fee 改为按 `maturity - latestFTime`

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest -q tests/boros_v4_convergence_test.py tests/boros_v4_backtest_test.py`
- `/Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/demeter/boros_v4/tests -p 'test_*.py'`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- `findex_updated` 目前只确认并使用了 `latestFTime`，floating/fee index payload 结构还未完整恢复
- orderbook `market_orders_filled` 的极端尾部仍需进一步用协议状态约束
- settlement fee 仍是 proxy

next_step:
- 继续拆 `findex_updated` 的其余字段，优先确认 floating index / fee index
- 用解出的 window/index 状态进一步约束 near-maturity 的 orderbook 长尾 implied rate
