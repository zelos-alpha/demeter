goal: push the full execution prototype one step closer to boros `SwapMath` by aggregating same-timestamp fills into a combined execution quote

changes:
- added full-execution quote builders in `market.py`
- a full quote now:
  - filters by requested trade side when possible
  - aggregates same-timestamp fills
  - computes weighted fixed rate, weighted opening fee rate, and total execution fee
  - emits `split_fill` when both orderbook and amm participate
- updated `FundingConvergenceStrategy` to consume quote-based full execution instead of a single raw fill row
- added tests covering direction-aware full execution quotes and split-fill compatibility

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/tests -p 'boros_v4_*test.py'`
- result: `19 tests OK`

known gaps:
- this is still a replay-time quote combiner, not full book state reconstruction
- I stopped the end-to-end benchmark rerun after code validation so this step could be committed cleanly; next round should rerun the compare script to quantify the new drift/runtime tradeoff

next step:
- rerun `78_boros_full_execution_compare.py`
- decide whether quote aggregation should become the default full execution path
