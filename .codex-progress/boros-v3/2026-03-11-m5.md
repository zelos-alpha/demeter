goal: 用 `latestFTime` 恢复成交 fixed rate，压 orderbook near-maturity 的年化长尾

changes:
- `helper.py` 新增 `findex_updated` 的通用 frame 构建
- 事件成交解码时，`implied_rate` 和 `opening_fee_rate_annualized` 改为按 `maturity - latestFTime`
- tx ledger 追加 `latest_f_time` 和 `findex_sequence`
- convergence fixture 同步到 `latestFTime` 口径

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest -q tests/boros_v4_convergence_test.py tests/boros_v4_backtest_test.py`
- `/Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/demeter/boros_v4/tests -p 'test_*.py'`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- `findex_updated` 里除 `latestFTime` 之外的 index 字段还没确认
- settlement fee 仍是 proxy
- 双市场执行仍是 taker-only experimental

next_step:
- 继续拆 `findex_updated` 的 raw index 字段，优先确认 floating index / fee index 的真实来源
- 把这些字段接进 state frame，替换当前的 continuous proxy index
