goal: 接入真实执行费，并把 settlement fee 与 opening fee 从事件链路里拆开

changes:
- 在 `helper.py` 中为事件成交解出 `opening_fee_rate_annualized`，并聚合到 tx ledger
- 将 `fee_rate_updated` 统一改为 `settlement_fee_rate_annualized_proxy`，不再混作 opening fee
- 在 `market.py` 的开仓和平仓接口中接入 `execution_fee_paid`
- 平仓时把真实执行费从 realized pnl 中扣除
- 在 `analysis.py` 的 summary/report 中导出市场级和组合级 `total_execution_fees`

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest -q tests/boros_v4_convergence_test.py tests/boros_v4_backtest_test.py`
- `/Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/demeter/boros_v4/tests -p 'test_*.py'`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- `market_orders_filled` 的协议语义和缩放仍未完全确认
- settlement fee 仍是事件驱动 proxy，不是完整链上恢复
- `tx_replay_best_exec` 仍是 taker-only，没有完整 orderbook queue / maker replay

next_step:
- 校正 orderbook `market_orders_filled` 的 trade value / fee 口径
- 继续把 `findex_updated` 和 settlement timeline 推向更明确的状态机
