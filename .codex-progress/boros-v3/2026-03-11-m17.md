goal: align current Boros engine with colleague boros branch protocol primitives and document remaining protocol gaps

changes:
- merged colleague-branch Boros protocol primitives into the public API surface:
  - `Side`
  - `TimeInForce`
  - `Trade`
  - `Fill`
- aligned `FixedFloatDirection` with Boros `Side`
  - `PAY_FIXED -> LONG`
  - `RECEIVE_FIXED -> SHORT`
- switched signed-size conversion to use Boros `Side.to_signed_size(...)`
- added `trade_side` columns to decoded trade ledgers and tx ledgers
- added protocol-model metadata to convergence summaries:
  - `execution_model`
  - `settlement_index_model`
  - `mark_rate_model`
  - `protocol_alignment_mode`
- documented the biggest remaining protocol mismatch explicitly:
  - current `mark_rate` is still a traded implied-rate proxy, not a full
    `markRateView` implementation

tests:
- boros_v4 unit/integration test suite

known gaps:
- full book/AMM execution path from colleague branch is still incomplete and not
  yet integrated
- maker zero-fee behavior is still outside the live engine because execution is
  taker-only
- full mark-rate observation-window logic is not implemented yet

next step:
- compare current replay engine against a minimal full-Boros execution path
- benchmark runtime and output drift to decide whether full execution is worth
  the speed cost
