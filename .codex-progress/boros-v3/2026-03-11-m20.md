goal: make the full execution prototype more protocol-like by selecting better book/amm fills at the earliest executable timestamp

changes:
- added scored full-execution candidate selection in `market.py`
- at a shared earliest execution timestamp, the prototype now prefers:
  - lower all-in fixed rate for `PAY_FIXED` opens
  - higher net fixed rate for `RECEIVE_FIXED` opens
  - corresponding favorable exit-side rates on close
- wired `FundingConvergenceStrategy` to pass direction-aware rate preferences into the full execution path
- updated convergence tests to validate direction-aware execution selection
- reran the full execution compare benchmark and refreshed comparison outputs

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/tests -p 'boros_v4_*test.py'`
- result: `19 tests OK`
- benchmark rerun:
  - baseline `tx_replay_best_exec`: final net value `996.1757`
  - full execution prototype: final net value `1000.1179`

known gaps:
- fill scoring is still a pragmatic replay heuristic, not the full `TradeModule._swap_book_amm()` path
- comparison script still uses manually supplied runtime values from the latest run summary step

next step:
- continue pulling in execution semantics from `TradeModule` where they improve fill realism without forcing a full orderbook engine rewrite
