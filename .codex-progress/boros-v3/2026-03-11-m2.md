goal: stabilize the dual-market convergence signal by excluding near-maturity and implausible-rate bars

changes:
- added `min_time_to_maturity_seconds` and `max_signal_rate` guards to FundingConvergenceStrategy
- signal window now only learns from `signal_ready` bars
- spread reports now include `signal_ready_count`, `signal_ready_spread_mean`, and `signal_ready_spread_max_abs`
- updated the real-data sample to use the new signal guards

tests:
- /Users/hou/Desktop/demeter/.venv/bin/python -m unittest -q tests/boros_v4_convergence_test.py tests/boros_v4_backtest_test.py
- PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python samples/strategy-example/72_boros_funding_convergence.py

known gaps:
- the raw `spread_last` tail is still explosive because the event decoder preserves true near-maturity annualization
- the strategy now guards against those bars, but the decoder scaling/semantics still need more protocol validation
- settlement fee semantics still use a best-effort proxy

next step:
- continue aligning orderbook event decoding with Pendle/Boros semantics
- improve settlement fee reconstruction from real events
- decide whether to publish both raw spread stats and signal-ready spread stats in the final report format
