goal: absorb protocol-level pieces from the original boros branch, starting with math helpers and their tests

changes:
- fixed `SwapMath.py` to use package-relative imports and consistent protocol-side types
- fixed `AMM.swap_view()` to return the fee-adjusted cost value instead of a tuple
- added minimal defaults for `OrderId` and `CancelData.empty()` in `_typing.py`
- fixed a broken `TickSweepStateLib` type annotation that prevented module import on Python 3.13
- added `tests/boros_v4_protocol_math_test.py` to lock:
  - trade/fill side semantics
  - payment/fee formulas
  - swap math rate conversions
  - protocol helper importability

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest discover -s /Users/hou/Desktop/demeter/tests -p 'boros_v4_*test.py'`
- result: `18 tests OK`

known gaps:
- `TradeModule` is importable, but its full orderbook/amm execution flow is still incomplete
- these changes harden the protocol math layer; they do not yet replace the current replay engine

next step:
- compare `SwapMath` / `TradeModule` behavior against the original boros design and selectively bring in execution logic where it improves the full execution prototype
