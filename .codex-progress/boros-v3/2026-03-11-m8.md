goal: 把 dual-market settlement 的 floating index 从“窗口内连续成交积分”改为“按 latestFTime 窗口聚合推进”

changes:
- `helper.py` 的 `_build_state_frame(...)` 改为按 `latestFTime` window 聚合 rate
- 每个 settlement window 使用窗口聚合的 implied rate 推进 `floating_index`
- 清理了 pandas `groupby.apply` warning

tests:
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python -m unittest -q tests/boros_v4_convergence_test.py tests/boros_v4_backtest_test.py`
- `PYTHONPATH=/Users/hou/Desktop/demeter /Users/hou/Desktop/demeter/.venv/bin/python samples/strategy-example/72_boros_funding_convergence.py`

known_gaps:
- 仍未恢复 `findex_updated` 的真实 floating index / fee index 数值字段
- settlement fee 仍是 proxy
- 订单簿和 AMM 仍是 taker-only experimental replay

next_step:
- 若 `findex_updated` 无法稳定反解真实 index，考虑显式暴露 `window_settlement_mode` 作为 experimental 执行/估值选项
- 继续研究 Hyperliquid 末端窗口的极端 rate 样本
