# Demeter 项目审查需求文档

> 文档类型：审查需求规格
> 创建日期：2026-06-11
> 状态：需求定义阶段

---

## 一、项目背景

### 1.1 项目简介

Demeter 是一个 DeFi（去中心化金融）回测框架，为量化策略开发者提供协议级别的模拟环境。框架支持多种 DeFi 协议的回测，包括：

- **Uniswap V3**：集中流动性做市策略回测
- **Aave V3**：借贷协议策略回测
- **Deribit**：期权交易与希腊值对冲策略回测
- **GMX V1/V2**：永续合约与 GLP LP 策略回测
- **Boros**：固定利率/浮动利率互换策略回测

### 1.2 技术栈

- 语言：Python 3.11+
- 核心依赖：pandas、numpy、Decimal 精度计算
- 数据处理：分钟级时间序列数据
- 打包：setuptools（setup.py）
- 测试：pytest

### 1.3 审查背景

项目经历多轮迭代，陆续新增了 GMX、Boros 等多个协议模块，由不同开发者在不同时期贡献。随着代码规模增长，项目中积累了命名不一致、架构耦合、工程规范缺失等问题。本次审查旨在系统性地识别这些问题，并为后续重构提供方向。

---

## 二、审查目标

本次审查的核心目标是：**系统性地识别 demeter 项目中不合理、不规范、存在风险的地方，输出一份可执行的改进需求清单**。

具体目标包括：

1. 发现影响回测正确性的逻辑 Bug
2. 识别降低代码可读性和可维护性的设计问题
3. 评估项目工程化水平与现代 Python 最佳实践的差距
4. 检验测试体系的完备性和组织合理性
5. 为后续改进工作提供优先级排序和执行路线图

---

## 三、审查维度

本次审查从以下 **7 个维度** 展开，每个维度聚焦一类"不合理"：

### 3.1 正确性审查（Correctness）

**关注点**：代码逻辑是否存在 Bug，是否会导致回测结果不准确或运行时崩溃。

**审查范围**：
- 条件判断的逻辑正确性（边界条件、空值处理）
- 变量引用的有效性（未定义变量、作用域问题）
- 异常处理的完整性（异常捕获是否遗漏关键场景）
- 抽象方法/接口的一致性（子类是否正确实现父类契约）
- 数值计算的精度问题（Decimal 与 float 混用、精度丢失）

**审查产出要求**：逐条列出发现的 Bug，标注影响范围（影响哪些协议/功能），给出修复建议。

### 3.2 命名与可读性审查（Naming & Readability）

**关注点**：文件名、模块名、变量名、注释是否存在拼写错误或误导信息，是否符合 Python 命名规范。

**审查范围**：
- 文件命名是否符合 PEP 8（snake_case 模块名）
- 文件名拼写是否正确
- 变量/函数命名是否准确反映其用途
- 注释是否与代码逻辑一致
- 模块级 docstring 是否准确描述模块功能

**审查产出要求**：列出所有命名不规范或存在误导的项，给出规范化的命名建议。

### 3.3 代码整洁度审查（Code Cleanliness）

**关注点**：代码中是否存在冗余、死代码、重复定义等影响可维护性的问题。

**审查范围**：
- 无意义的 `pass` 语句
- 重复的变量/类定义
- 未使用的函数、导入、变量
- 空逻辑块（if/else 分支为空）
- 调试残留代码（print、注释掉的代码块）

**审查产出要求**：按文件列出需要清理的具体位置，标注类型（冗余 pass / 重复定义 / 死代码等）。

### 3.4 架构合理性审查（Architecture）

**关注点**：模块组织、类设计、代码复用、耦合关系是否存在明显不合理之处。

**审查范围**：
- 模块间是否存在不必要的耦合或循环依赖
- 是否存在可以提取公共基类/工具函数的重复代码
- 类的属性管理方式是否合理（动态 vs 静态声明）
- 全局状态的使用是否安全（多进程场景）
- 新增协议模块是否遵循了统一的设计模式

**审查产出要求**：对每个架构问题给出"当前现状"与"期望状态"的对比，以及迁移路径建议。

### 3.5 工程规范审查（Engineering Standards）

**关注点**：项目的打包方式、依赖管理、版本控制是否符合现代 Python 工程最佳实践。

**审查范围**：
- 打包配置是否使用现代工具（pyproject.toml vs setup.py）
- 依赖声明是否精简（是否存在不必要的依赖）
- 依赖来源是否统一（setup.py vs requirements.txt 是否同步）
- 敏感信息（.pypirc、密钥等）是否被正确排除在版本控制之外
- 根目录是否包含不属于项目的杂项文件
- .gitignore 规则是否完善

**审查产出要求**：列出需要调整的工程规范项，给出具体的迁移方案。

### 3.6 测试完备性审查（Testing）

**关注点**：测试体系是否完备，测试文件的组织和命名是否合理。

**审查范围**：
- 测试文件命名是否统一（pytest 标准 vs 自定义后缀）
- 测试代码是否集中管理（分散在多处的测试目录）
- 测试配置是否完善（conftest.py、pytest 配置）
- 非测试文件是否混入测试目录
- 空测试文件或桩文件是否需要清理

**审查产出要求**：给出测试体系的整改方案，包括文件重命名、目录合并、配置补充等。

### 3.7 可维护性审查（Maintainability）

**关注点**：代码是否有利于长期维护，包括异常处理、日志、文档、类型标注等。

**审查范围**：
- 异常类型使用是否语义准确（AssertionError 用于业务逻辑等）
- 日志系统是否统一使用（print vs logging）
- 公共 API 是否有完整的 docstring
- 类型标注是否准确（返回类型、参数类型）
- 序列化方式是否存在安全风险（pickle 反序列化）

**审查产出要求**：列出需要补充或修正的文档、日志、异常处理项。

---

## 四、交付物要求

本次审查最终交付以下内容：

### 4.1 主交付物：审查报告（PROJECT_REVIEW.md）

包含以下章节：

| 章节 | 内容 | 说明 |
|------|------|------|
| 项目背景 | 项目简介、技术栈、审查背景 | 帮助读者理解项目全貌 |
| 审查目标 | 本次审查的核心目标 | 明确审查的边界和期望 |
| 审查维度 | 7 个维度的定义和关注点 | 说明审查的系统性框架 |
| 问题清单 | 按维度分类的具体问题 | 每个问题附带证据和改进建议 |
| 改进优先级 | P0~P3 四级优先级排序 | 帮助团队决定先修什么 |
| 执行路线图 | 分阶段的修改计划 | 估算时间、依赖关系 |

### 4.2 问题清单格式

每个问题条目应包含：

- **问题编号**：唯一标识（如 D1-001）
- **问题描述**：一句话概括问题本质
- **问题位置**：精确到文件和行号
- **问题证据**：相关代码片段或现象描述
- **影响评估**：该问题的影响范围和严重程度
- **改进方向**：一句话概括应如何修复（不涉及具体代码）

### 4.3 优先级定义

| 优先级 | 定义 | 预期修复时间 |
|--------|------|------------|
| P0（紧急） | 影响回测正确性，可能导致错误结果或运行崩溃 | 1-2 天 |
| P1（高） | 严重影响代码可读性和可维护性 | 3-5 天 |
| P2（中） | 架构层面的改进，影响长期维护效率 | 1-2 周 |
| P3（低） | 完善性工作，提升工程规范和文档质量 | 持续进行 |

### 4.4 不要求的交付物

- 不需要给出具体的代码修改（只需给出方向和思路）
- 不需要编写测试代码
- 不需要实际执行修改操作
- 不需要修改任何现有文件

---

## 五、审查约束与注意事项

### 5.1 审查原则

- **客观性**：基于代码事实发现问题，不做主观臆测
- **系统性**：按维度逐项检查，不遗漏
- **可操作性**：每个问题都应有明确的改进方向
- **优先级导向**：区分"必须修"和"建议改"

### 5.2 审查边界

- 审查范围：`demeter/` 包全部代码、`tests/` 目录、项目根目录配置文件
- 不审查：`samples/`、`docs/` 的内容质量（仅检查是否需要清理）
- 不审查：性能优化问题（属于另一个专项）
- 不审查：新功能需求（仅审查现有代码）

### 5.3 已知的审查约束

- 项目无 CI/CD 配置，无法通过自动化工具辅助审查
- 项目无类型检查配置（mypy），类型相关问题需人工识别
- 部分模块（如 boros_v4）的业务逻辑复杂，审查侧重结构问题而非算法正确性

---

## 六、审查工作大纲

以下是执行审查的具体工作分解：

### Step 1：项目结构梳理（0.5 天）
- 通读项目目录结构，理解模块划分
- 阅读 `setup.py`、`requirements.txt` 了解依赖关系
- 阅读各 `__init__.py` 了解模块导出关系
- 产出：项目架构概览图

### Step 2：核心模块审查（2 天）
- 审查 `core/`（回测引擎核心逻辑）
- 审查 `broker/`（资产管理与市场抽象）
- 审查 `strategy/`（策略基类与触发器）
- 审查 `_typing.py`（全局类型定义）
- 产出：核心模块问题清单

### Step 3：协议模块审查（2 天）
- 审查 `uniswap/`（Uniswap V3 市场实现）
- 审查 `aave/`（Aave V3 市场实现）
- 审查 `deribit/`（Deribit 期权市场实现）
- 审查 `gmx/`（GMX V1/V2 市场实现）
- 审查 `boros_v4/`（Boros 市场实现）
- 产出：各协议模块问题清单

### Step 4：工程规范审查（0.5 天）
- 检查打包配置（setup.py / setup.cfg / pyproject.toml）
- 检查依赖声明完整性
- 检查 .gitignore 规则完善性
- 检查根目录文件整洁度
- 产出：工程规范问题清单

### Step 5：测试体系审查（0.5 天）
- 检查测试文件命名和组织
- 检查测试配置完整性
- 检查测试覆盖情况
- 产出：测试体系问题清单

### Step 6：文档与可维护性审查（0.5 天）
- 检查公共 API docstring 覆盖情况
- 检查异常类型使用合理性
- 检查日志系统使用一致性
- 产出：可维护性问题清单

### Step 7：汇总与报告编写（0.5 天）
- 汇总所有维度的问题清单
- 按优先级排序
- 编写最终审查报告
- 产出：`PROJECT_REVIEW.md`

---

## 七、成功标准

本次审查完成的标志是：

1. ✅ 7 个审查维度全部覆盖
2. ✅ 每个维度至少发现 2 个以上具体问题
3. ✅ 所有问题附带精确的文件位置和证据
4. ✅ 所有问题有明确的改进方向
5. ✅ 问题按 P0~P3 优先级排序
6. ✅ 有分阶段的执行路线图
7. ✅ 文档结构清晰，可直接用于团队评审
